import React from 'react'

function Showroom() {
    const brand = ["Audi", "TATA", "Merceedez", "Mustang"];
    console.log(brand);
  return (
    <div>   
        <ol>
        {/* brand.map((brand) => { */}

        {/* }) */}
        </ol>
    </div>
  )
}

export default Showroom